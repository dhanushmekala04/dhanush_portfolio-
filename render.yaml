services:
  - type: web
    name: dhanush-portfolio
    runtime: node
    buildCommand: npm run build
    startCommand: npm start
    publishDir: ./build
    envVars:
      - key: NODE_ENV
        value: production
